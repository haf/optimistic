// import { selectThreadPageDomain } from '../selectors';
// import { fromJS } from 'immutable';
import expect from 'expect';

// const selector = selectThreadPageDomain();

describe('selectThreadPageDomain', () => {
  it('unit test TODO', () => {
  });
});
